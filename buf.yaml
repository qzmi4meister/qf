version: v2
modules:
  - path: proto
    name: buf.build/qf/qf
lint:
  use:
    - STANDARD
  except:
    # AgentMessage/ServerMessage are intentional bidi-stream envelope names.
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
